# Base URL the API client prefixes onto every request path.
# In production the SPA and API share an origin (see docs/adr/ADR-001-server-owned-cart.md),
# so requests are path-routed under /api. Override for local dev if the backend
# runs on a different origin.
VITE_API_BASE_URL=/api
