name: CI

# The gate that has to go red before a human is asked to read the diff:
# lint, type-check, test and image build for both services.
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  # `gh workflow run ci.yml` needs the workflow to accept a manual trigger.
  workflow_dispatch:

# One in-flight run per branch. A new push supersedes the run it invalidated.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# Explicit `bash`, which GitHub runs as `bash -e -o pipefail`: a step fails on
# the first non-zero exit, and a failing command in a pipeline is not masked by
# a succeeding one after it.
defaults:
  run:
    shell: bash

jobs:
  backend:
    name: Backend — ruff + pytest
    runs-on: ubuntu-latest
    defaults:
      run:
        # The service lives in backend/; a run from the repo root finds nothing.
        working-directory: backend

    # Real Postgres and Redis, not mocks of them. Same images and the same
    # health probes as docker-compose.yml, so a test that passes here passes
    # against the local stack too.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: qbiq
          POSTGRES_PASSWORD: qbiq
          POSTGRES_DB: qbiq_h
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U qbiq -d qbiq_h"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
          --health-start-period 10s
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      # Point the app at the service containers. Both are published on the
      # runner's loopback interface because the job's steps run on the host,
      # not inside a job container.
      DATABASE_URL: postgresql://qbiq:qbiq@localhost:5432/qbiq_h
      REDIS_URL: redis://localhost:6379/0
      # ALLOWED_ORIGINS is deliberately left unset: tests/test_error_handling.py
      # asserts against the built-in default origin.
      PIP_DISABLE_PIP_VERSION_CHECK: "1"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # Matches the `python:3.12-slim` base in backend/Dockerfile.
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Install dependencies
        # requirements-dev.txt pulls in requirements.txt and adds pytest and
        # httpx, which the runtime image deliberately does not ship.
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Install ruff
        # Pinned in the workflow rather than in requirements-dev.txt so the lint
        # gate cannot drift under the codebase when a new ruff relaxes or adds
        # a rule, and so the runtime dependency set stays untouched.
        run: pip install ruff==0.16.2

      - name: Lint (ruff)
        run: ruff check .

      - name: Wait for Postgres and Redis to accept connections
        # The container health checks gate the job start, but a green health
        # check on a published port is not proof the test process can reach it.
        # Connecting here turns "service missing" into a clear failure rather
        # than a confusing test error, and proves the tests below run against
        # live servers.
        run: |
          python - <<'PY'
          import os
          import time

          import psycopg
          import redis

          deadline = time.monotonic() + 60

          while True:
              try:
                  with psycopg.connect(os.environ["DATABASE_URL"], connect_timeout=5) as conn:
                      version = conn.execute("SELECT version()").fetchone()[0]
                  print(f"postgres ready: {version}")
                  break
              except Exception as exc:
                  if time.monotonic() > deadline:
                      raise SystemExit(f"postgres never became reachable: {exc}")
                  time.sleep(2)

          while True:
              try:
                  client = redis.Redis.from_url(os.environ["REDIS_URL"], socket_connect_timeout=5)
                  client.ping()
                  print(f"redis ready: {client.info('server')['redis_version']}")
                  break
              except Exception as exc:
                  if time.monotonic() > deadline:
                      raise SystemExit(f"redis never became reachable: {exc}")
                  time.sleep(2)
          PY

      - name: Test (pytest)
        run: pytest -q

  frontend:
    name: Frontend — eslint + vue-tsc + vitest + build
    runs-on: ubuntu-latest
    defaults:
      run:
        # The SPA lives in frontend/.
        working-directory: frontend

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          # Matches the `node:22-alpine` base in frontend/Dockerfile.
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        # `npm ci`, not `npm install`: the lockfile is the input, and a lockfile
        # that has drifted from package.json is itself a failure.
        run: npm ci

      - name: Lint (eslint)
        # `npm run lint` carries `--max-warnings 0`. Without it the gate can
        # never fail: every rule in eslint-plugin-vue's flat/recommended is a
        # warning, so a file full of violations still exits 0.
        run: npm run lint

      - name: Type-check (vue-tsc)
        # Run as its own gate so a type error is reported as a type error rather
        # than as a build failure. `npm run build` re-runs it; the second run is
        # incremental and near-free.
        run: npx vue-tsc -b --force

      - name: Test (vitest)
        run: npm run test

      - name: Build
        run: npm run build

  images:
    name: Images — docker build
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Build the backend image
        # Build context is backend/, matching docker-compose.yml. The image is
        # built, not pushed: this stage only proves the Dockerfile still builds.
        run: docker build --pull -t qbiq-h-backend:${{ github.sha }} backend

      - name: Build the frontend image
        # The frontend Dockerfile runs `npm run build` inside the builder stage,
        # so a type error fails this job as well as the frontend job.
        run: docker build --pull -t qbiq-h-frontend:${{ github.sha }} frontend
